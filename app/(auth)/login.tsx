/**
 * LoginRoute
 *
 * Route file for the login screen.
 * Thin wrapper — all logic and UI lives in features/auth/screens/LoginScreen.
 */

import { LoginScreen } from "@/features/auth/screens/LoginScreen";

export default function LoginRoute() {
  return <LoginScreen />;
}
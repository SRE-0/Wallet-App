/**
 * MainRoute
 *
 * Route file for the main dashboard screen.
 * Thin wrapper — all logic and UI lives in features/home/screens/main.
 */

import { Main } from "@/features/home/screens/main";

export default function MainRoute() {
  return <Main />;
}